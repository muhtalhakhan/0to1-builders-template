# deploy

Use when user says `$deploy`.

## workflow
1. Explain that Play Store/App Store publishing needs developer accounts and manual console steps.
2. Build local release artifacts (`flutter build apk` or `flutter build appbundle`).
3. Provide plain-language next steps for store submission, without claiming auto-deploy.
